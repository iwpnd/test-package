name: test-release
on:
  push:
    branches:
    - main
  pull_request:

permissions:
  id-token: write
  contents: read

jobs:
  hello:
    name: say hello
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        uses: iwpnd/github-actions/actions/hello-world@v1.0.1
        with:
          name: Ben

  tag-and-release:
    name: tag-and-release
    runs-on: ubuntu-latest
    env:
      RUNNER_DEBUG: 1
    permissions:
      id-token: write
      contents: write
    if: github.event_name == 'push' # TODO: re-enable if issue#252 closed
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Semantic Release
        uses: iwpnd/semantic-release-action@v1.0.1
        with:
          extends: |
            github:@iwpnd/semantic-release-config#semver:1.0.5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
